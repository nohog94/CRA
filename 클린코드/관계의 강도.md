```java
//1. use-a 관계 (//OOP : Dependency 관계)

import java.util.ArrayList;

class Node {
}

class TVMan {
    public void run1() { //use-a 관계 1
        Node t = new Node();
    }

    public void run2(Node v) { //use-a 관계 2
    }

    public Node factory() { //use-a 관계 3
        return new Node();
    }
}

//2. is-a 관계
//상속 - 상위분류, 하위분류 (분류 : Class)

//3. has-a 관계
class Coffee {}

class SpeakerMan {
    public void buy(Coffee c) { //has- a 1 (Assocication 관계)
        this.myCoffe = c;
    }
    Coffee myCoffe;

    public void buy2(Coffee cc) { //has-a 2 (Aggregation 관계)
        mine.add(cc);
    }
    ArrayList<Coffee> mine = new ArrayList<>();

    //has-a 3 (Composition 관계)
    Coffee onlyMine = new Coffee(); //스피커맨이 태어나자마자 커피를 갖고 태어남
                                    //스피커맨 Die할때 소멸
}

//Realizaion <-- interface 생략
```